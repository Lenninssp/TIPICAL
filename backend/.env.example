# Backend auth
AUTH_SECRET=replace-with-strong-random-secret
AUTH_TOKEN_EXPIRATION_MS=86400000

# Firebase
FIREBASE_PROJECT_ID=tipical-bd8e7

# Optional CORS override (comma-separated)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,https://lenninsabogal.online
